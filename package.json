{
  "name": "yatzygame",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "ng build --aot --prod --output-path=../resources/static/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "run-s build:npm build:ng",
    "build:npm": "npm install && npm run postinstall",
    "build:ng": "ng build --aot --prod"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "java": "^0.12.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
